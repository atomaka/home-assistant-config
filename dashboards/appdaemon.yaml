log:
  logfile: STDOUT
  errorfile: STDERR
appdaemon:
  threads: 10
  plugins:
    HASS:
      type: hass
      ha_url: !secret ha_url
      ha_key: !secret ha_key
hadashboard:
  dash_password: !secret dash_password
  dash_url: !secret dash_url
